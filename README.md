# Security Questionnaire Sync

This project provides a Python implementation for automating the syncing of security questionnaires.

## Requirements

* Python 3.8+
* No external dependencies required

## Usage

1. Run the main function using `python -m security_questionnaire_sync`
2. The program will connect to the API, sync the security questionnaires, and print the result in JSON format
